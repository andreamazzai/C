#include <stdio.h>
#include <stdint.h>

int main() {
    // Utilizza sizeof per ottenere la dimensione di uint32_t in byte
    printf("La dimensione di uint32_t è: %lu byte.\n", sizeof(uint32_t));

    return 0;
}
